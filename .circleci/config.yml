version: 2

jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:12.18

    steps:
      - checkout
      - run: cd api && npm install
      - run: cd api && npm test
